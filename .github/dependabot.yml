# Basic dependabot.yml file with
# minimum configuration for two package managers

version: 2
updates:
  # Enable version updates for npm
      
  - package-ecosystem: "nuget"
    directory: "/" #AzureIdentityStorageExample.csproj
    schedule:
      interval: "daily"
    
